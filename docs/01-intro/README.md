# Тема 1: Огляд фреймворку Cypress

## Цілі навчання

Після вивчення цієї теми ви зможете:

- Зрозуміти, що таке Cypress та його призначення
- Ознайомитися з основними можливостями та перевагами фреймворку
- Зрозуміти різницю між E2E та компонентними тестами
- Створити свій перший тест у Cypress

## Що таке Cypress?

Cypress - це сучасний тестовий фреймворк для автоматизації тестування веб-додатків. Він надає зручний інтерфейс для розробки тестів, високу продуктивність, зручну систему візуалізації, а також вбудовану підтримку для відлагодження тестів.

### Основні особливості Cypress

1. **Дружній для розробників інтерфейс**
   - Простий та зрозумілий синтаксис
   - Інтуїтивний API для взаємодії з веб-елементами

2. **Автоматичне очікування**
   - Cypress автоматично очікує дій користувача та подій у додатку
   - Не потрібно вручну додавати затримки або чекати елементи

3. **Широкі можливості відлагодження**
   - Вбудований інструмент для відлагодження тестів
   - Можливість додавати точки зупинки
   - Спостереження за змінними стану

4. **Візуальне тестування**
   - Можливість виконувати візуальне тестування в реальному часі
   - Автоматичні снепшоти на кожну дію

5. **Підтримка асинхронних операцій**
   - Гарантує правильну послідовність виконання тестів
   - Працює з асинхронними операціями у веб-додатку

6. **Робота з DOM**
   - Простий доступ до DOM-елементів
   - Можливість маніпулювати елементами під час тестування

## Основні складові Cypress

### Mocha тестовий фреймворк

Cypress використовує Mocha для написання тестових сценаріїв. Це означає, що ви використовуєте знайомі конструкції:
- `describe()` - для групування тестів
- `it()` - для опису окремих тестових випадків
- `before()`, `beforeEach()`, `after()`, `afterEach()` - хуки життєвого циклу

### Chai бібліотека для перевірки

Cypress використовує Chai для зручної перевірки тестових умов:
- `should()` - для асертів
- `expect()` - для перевірок
- Богатий набір асертів (visible, exist, contain, equal тощо)

### jQuery-подібні селектори

Cypress використовує синтаксис селекторів, схожий на jQuery:
- `cy.get('.class-name')` - пошук за класом
- `cy.get('#id-name')` - пошук за ID
- `cy.get('tag-name')` - пошук за тегом

### Cypress Dashboard

Платформа для збереження результатів тестів та їх аналізу (потребує окремого налаштування).

## Області застосування

### End-to-End (E2E) тести

E2E тести повністю відтворюють взаємодію користувача з веб-додатком:

- **Повне відтворення взаємодії користувача**
  - Навігація по сайту
  - Взаємодія з елементами
  - Перевірка стану додатку

- **Синхронізація з автоматичним очікуванням**
  - Вбудоване автоматичне очікування дозволяє Cypress очікувати стану елементів
  - Спрощує написання тестів E2E

- **Візуальне тестування**
  - Можливість проводити візуальні тести в реальному часі
  - Перевірка візуальних змін у додатку

### Компонентні (модульні) тести

Cypress може бути використаний для тестування окремих компонентів:

- **Підхід до тестування окремих компонентів**
  - Тестування компонентів ізольовано від інших частин додатку
  - Перевірка функціоналу окремих частин

- **Маніпулювання з DOM-елементами**
  - Зручний спосіб маніпулювати з DOM-елементами у компонентних тестах
  - Перевірка структури компонентів

## Демо-сайт для навчання

У цьому курсі ми використовуємо **TodoMVC React** як демо-сайт для практичних прикладів:

🔗 [TodoMVC React](https://todomvc.com/examples/react/dist/)

### Функціонал TodoMVC:

- Додавання нових задач через поле вводу
- Відмітка задач як виконаних/невиконаних
- Видалення окремих задач
- Фільтрація задач (All, Active, Completed)
- Очищення всіх виконаних задач
- Редагування задач (подвійний клік)
- Підрахунок активних задач

## Практичні приклади

У папці `examples/` знаходяться робочі приклади коду:

- `basic-test.cy.js` - базовий тест для демонстрації
- `e2e-vs-component.cy.js` - порівняння підходів

## Типові помилки

1. **Неправильне розуміння різниці між E2E та компонентними тестами**
   - E2E тести перевіряють весь додаток як ціле
   - Компонентні тести перевіряють окремі частини ізольовано

2. **Очікування, що Cypress працює з усіма браузерами**
   - Cypress не підтримує Safari
   - Підтримує Chrome, Firefox, Edge, Electron

## Наступні кроки

Після вивчення цієї теми перейдіть до:
- [Тема 2: Встановлення та початкова конфігурація](../02-setup/README.md)
