// Рішення вправи 1: Створення першого тесту

describe('Перший тест TodoMVC', () => {
  it('має відкрити сторінку та перевірити основні елементи', () => {
    // Крок 1: Відкриваємо TodoMVC
    cy.visit('/')
    
    // Крок 2: Перевіряємо заголовок
    cy.get('h1').should('contain', 'todos')
    
    // Крок 3: Перевіряємо поле вводу
    cy.get('.new-todo').should('be.visible')
  })
})
