# Покрокова інструкція встановлення Cypress

## Крок 1: Перевірка передумов

Перевірте, що у вас встановлено необхідне ПЗ:

```bash
node --version  # Має бути >= 16.x
npm --version   # Має бути >= 8.x
```

## Крок 2: Ініціалізація проекту

Якщо у вас ще немає `package.json`:

```bash
npm init -y
```

## Крок 3: Встановлення Cypress

Встановіть Cypress як dev-залежність:

```bash
npm install cypress --save-dev
```

## Крок 4: Перевірка встановлення

Перевірте, що Cypress встановлено правильно:

```bash
npx cypress verify
```

## Крок 5: Ініціалізація Cypress проекту

Запустіть Cypress UI:

```bash
npx cypress open
```

Або додайте скрипт у `package.json`:

```json
{
  "scripts": {
    "cypress:open": "cypress open"
  }
}
```

## Крок 6: Вибір типу тестування

При першому запуску оберіть **E2E Testing**.

## Крок 7: Налаштування baseUrl

Відредагуйте `cypress.config.js`:

```javascript
const { defineConfig } = require('cypress')

module.exports = defineConfig({
  baseUrl: 'https://todomvc.com/examples/react/dist/',
  e2e: {
    setupNodeEvents(on, config) {
      // налаштування
    },
  },
})
```

## Крок 8: Створення першого тесту

Створіть файл `cypress/e2e/first-test.cy.js`:

```javascript
describe('Мій перший тест', () => {
  it('має відкрити TodoMVC', () => {
    cy.visit('/')
    cy.get('h1').should('contain', 'todos')
  })
})
```

## Крок 9: Запуск тесту

1. Відкрийте Cypress UI: `npm run cypress:open`
2. Оберіть браузер
3. Клікніть на тест `first-test.cy.js`
4. Спостерігайте за виконанням

## Готово! 🎉

Тепер у вас є працюючий Cypress проект!
